#include<stdio.h>

int main()
{   
   int num;
   int digit;
   int sum=0;
   scanf("%d",&num);
   while(num!=0)
   {
     digit=num%10;
     sum=sum*10+digit;
     num=num/10;
     
   }
   printf("%d",sum);
   return 0;
}
