#include <stdio.h>
int main()
{
   int year;
   scanf("%d", &year);
   if (year%400==0)
   {
      printf("%d",year);
   }
   else if (year%100==0)
   {
      printf("0");
   }
   else if (year%4==0)
   {
      printf("%d",year);
   }
   else
   {
      printf("0");
   }
   return 0;
}
