#include<stdio.h>

int main()
{
    int num;
   int digit,hdigit=0,sum=0;
   scanf("%d",&num);
   while(num!=0)
   {
       digit=num%10;
       if(digit>hdigit)
       {
           hdigit=digit;
       }
       num=num/10;
   }
   printf("%d",hdigit);
   return 0;
}

